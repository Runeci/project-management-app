import { HeaderAnimateDirective } from './header-animate.directive';

describe('HeaderAnimateDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderAnimateDirective();
    expect(directive).toBeTruthy();
  });
});
